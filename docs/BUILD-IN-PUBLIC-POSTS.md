# Build-in-Public Post Drafts

Every draft generated by the Content Ritual goes here, regardless of whether it gets posted. Posting is a separate decision - this is the backlog.

---

## Day 1 — Database schema + Prisma setup

**X / Twitter:**
Shipped the database schema for my AI Spend Tracker today — Users, Teams
(many-to-many), usage records, and per-user + team budgets.

Weirdest bug: Windows Smart App Control silently blocked Prisma's
migration engine. Took an hour to realize it wasn't Prisma's fault.

Next: seeding test data + wiring up Clerk auth.

**LinkedIn:**
Ever wonder exactly how much your team is spending on Claude and GPT
each month, broken down by person? Most managers don't — the usage
just shows up as one line on a company card statement.

Today I designed and shipped the database schema for a tool that fixes
that: it tracks AI usage per employee, rolls it up by team, and lets
managers set budgets before spend gets out of hand — not after the
invoice arrives.

Honest moment: I lost almost as much time to environment setup as to
the actual schema design. A Windows security feature was silently
blocking my database migration tool, with an error message that gave
zero indication why. A good reminder that "building in public" includes
the boring debugging, not just the wins.

Building this in public — day 1 of 20.
